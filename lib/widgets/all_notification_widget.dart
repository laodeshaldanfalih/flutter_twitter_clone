import 'package:flutter/material.dart';
import 'package:testing2/widgets/widgets.dart';

class AllNotificationWidget extends StatelessWidget {
  const AllNotificationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TweetNotification();
  }
}
